import React from "react";
// import noteContext from "../context/notes/noteContext";
const About = () => {
  // const a = useContext(noteContext);
  return <div className="container">It's a note app with signup and signin authenication using react js and node js</div>;
};

export default About;
